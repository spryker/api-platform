parameters:
    ignoreErrors:
        -
            identifier: missingType.iterableValue
    level: 8
    # A @phpstan-ignore only matches under the default deps, not --prefer-lowest (non-ignorable otherwise).
    reportUnmatchedIgnoredErrors: false
